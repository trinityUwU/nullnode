node_modules
dist
logs
*.pid
.env

# tauri (build Rust)
src-tauri/target
src-tauri/gen

# relay
relay/node_modules
relay/logs
relay/*.pid
# Données utilisateur (enveloppes store-and-forward + backups chiffrés) — jamais publiées
relay/data
